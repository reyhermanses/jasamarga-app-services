export const sxMenuItemIco = (open: boolean) => {
    [
        open ? {
            backgroundColor: 'blue',
            // borderRadius: 2.5
            width: 32, height: 32
        } : {
            width: 32, height: 32
        }
    ]
}